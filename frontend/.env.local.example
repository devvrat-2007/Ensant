# Frontend environment template. Copy to `.env.local` and adjust if needed.
#   cp .env.local.example .env.local
#
# Backend API base URL. MUST use the same hostname the browser uses to load
# the frontend — mixing `localhost` and `127.0.0.1` causes "Failed to fetch".
NEXT_PUBLIC_API_URL=http://localhost:8000
