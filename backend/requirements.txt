# FlowZint backend dependencies — pinned to the versions verified in the venv.
# Regenerate with: pip freeze > requirements.txt

# ── Web framework ────────────────────────────────────────────────────────────
Django==6.0.5
djangorestframework==3.17.1
django-cors-headers==4.9.0

# ── Async task queue ─────────────────────────────────────────────────────────
celery==5.6.3
redis==8.0.0

# ── Vector DB + RAG ──────────────────────────────────────────────────────────
pinecone==7.3.0
pinecone-plugin-assistant==1.8.0
pinecone-plugin-interface==0.0.7
langchain==1.3.2
langchain-core==1.4.0
langchain-community==0.4.2
langchain-classic==1.0.7
langchain-google-genai==4.2.4
langchain-openai==1.2.2
langchain-pinecone==0.2.13
langchain-text-splitters==1.1.2
sentence-transformers==5.5.1

# ── AI providers ─────────────────────────────────────────────────────────────
google-genai==2.7.0

# ── Document processing ──────────────────────────────────────────────────────
pypdf==6.12.2

# ── Web search fallback ──────────────────────────────────────────────────────
duckduckgo_search==8.1.1

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
requests==2.34.2
psutil==7.2.2
tenacity==9.1.4

# ── Production deployment (install in prod environments only) ────────────────
# Not required for local development (SQLite + runserver). Uncomment / install
# these when deploying with Postgres behind a WSGI/ASGI server.
#   gunicorn>=23.0          # WSGI server
#   psycopg[binary]>=3.2    # PostgreSQL driver (enables DATABASE_URL=postgres://...)
#   whitenoise>=6.8         # static file serving (optional)
